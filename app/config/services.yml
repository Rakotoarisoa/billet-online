# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
parameters:
    #parameter_name: value

services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false
    app.form.flow.create_event:
        class: AppBundle\Form\CreateEventFormFlow
        autoconfigure: true
        public: true
    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name

    AppBundle\:
        resource: '../../src/AppBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/AppBundle/{Entity,Tests}'

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    AppBundle\Controller\:
        resource: '../../src/AppBundle/Controller'
        public: true
        tags: ['controller.service_arguments']
    # use the fully-qualified class name as the service id
    AppBundle\Utils\Slugger:
        public: false
    # add more services, or override services that need manual wiring
    # AppBundle\Service\ExampleService:
    #     arguments:
    #         $someArgument: 'some_value'
    AppBundle\Controller\RegistrationController:
        arguments:
            $formFactory: '@fos_user.registration.form.factory'
    app.form.registration:
        class: AppBundle\Form\RegistrationType
        tags:
            - { name: form.type, alias: app_user_registration }

        # ...
    twig.extension.intl:
        class: Twig_Extensions_Extension_Intl
        tags:
            - { name: twig.extension }
    admin.billet:
        class: AppBundle\Admin\BilletAdmin
        arguments: [~, AppBundle\Entity\Billet, ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: 'Billets', label: Billet }
    admin.typebillet:
        class: AppBundle\Admin\TypeBilletAdmin
        arguments: [~, AppBundle\Entity\TypeBillet, ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: 'Billets', label: 'Type Billet' }
    admin.categorieevenement:
        class: AppBundle\Admin\CategorieEvenementAdmin
        arguments: [~, AppBundle\Entity\CategorieEvenement, ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: 'Evenement', label: 'Catégorie evenement' }
    admin.lieuevenement:
        class: AppBundle\Admin\LieuEvenementAdmin
        arguments: [~, AppBundle\Entity\LieuEvenement, ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: 'Evenement', label: 'Lieu Evenement' }
    admin.evenement:
        class: AppBundle\Admin\EvenementAdmin
        arguments: [~, AppBundle\Entity\Evenement, ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: 'Evenement', label: 'Evenement' }
    admin.user:
        class: AppBundle\Admin\UserAdmin
        arguments: [~, AppBundle\Entity\User, ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: 'Utilisateurs et Droits', label: 'Utilisateurs' }
    admin.pays:
        class: AppBundle\Admin\PaysAdmin
        arguments: [~, AppBundle\Entity\Pays, ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: 'Configurations generales', label: 'Pays' }
    admin.devise:
        class: AppBundle\Admin\DeviseAdmin
        arguments: [~, AppBundle\Entity\Devise, ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: 'Configurations generales', label: 'Devise' }
    
            


